/**
 * Canonical Hardhat configuration entrypoint.
 *
 * Keep all config in hardhat.config.cjs and re-export here so tools that
 * default to hardhat.config.js keep working.
 */
module.exports = require("./hardhat.config.cjs");